{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "prisma generate && tsnd --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "db:deploy": "prisma reset --force --skip-seed && prisma migrate dev --name init && prisma db seed",
    "db:studio": "prisma studio",
    "db:gen": "prisma generate",
    "db:backup": "ts-node src/backup.ts",
    "db:seed": "prisma db seed",
    "db:reset": "prisma migrate reset --force --skip-seed",
    "db:dev": "prisma migrate dev --name init"
  },
  "prisma": {
    "seed": "ts-node src/seed.ts"
  },
  "keywords": [],
  "author": "Just_L & Dream",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "1.4.5-lts.2",
    "nodemailer": "^6.10.1",
    "sharp": "^0.34.2",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.18",
    "@types/express": "^5.0.2",
    "@types/jsonwebtoken": "^9.0.9",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.15.19",
    "@types/nodemailer": "^6.4.17",
    "prisma": "^6.8.2",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
